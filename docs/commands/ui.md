# ui *(deprecated)*

`memory ui` is deprecated and replaced by the **desktop app**.

```bash
memory ui
# → Use memory-desktop instead (https://github.com/Sakaax/memory/releases)
```

The daemon (`memory daemon start`) still runs for the browser extension — only the web interface has been replaced.

→ **[Desktop app documentation](../desktop.md)**
